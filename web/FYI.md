# Design Guidelines

- **Typography Rules:** Never use `Young Serif` (or the `.title-serif` class) for anything except the main navbar wordmark. For all other body text and headings throughout the site, stick exclusively to `Inter`, and rely on adjusting `font-size` and `font-weight` for visual hierarchy and emphasis.
